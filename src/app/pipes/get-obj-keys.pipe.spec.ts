import { GetObjKeysPipe } from './get-obj-keys.pipe';

describe('GetObjKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new GetObjKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
