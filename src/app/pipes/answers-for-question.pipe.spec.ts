import { AnswersForQuestionPipe } from './answers-for-question.pipe';

describe('AnswersForQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswersForQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
